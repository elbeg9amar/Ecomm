import React from 'react'
import {SpinnerOverlay,SpinnerContainer} from  '../spinner/spinner.styled';

const Spinner =() => (
    <SpinnerOverlay>
        <SpinnerContainer/>
    </SpinnerOverlay>
);

export default Spinner;
